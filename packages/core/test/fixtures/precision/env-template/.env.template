# Verbatim minimisation of ~/neat-experiment/bugs/0008-icons-to-env-template.md.
#
# .env.template is a documentation/onboarding artifact: developers copy it to
# .env and fill in real values. It has no runtime semantics. The v0.3.0
# extractor registered this as a ConfigNode and produced a CONFIGURED_BY edge,
# violating ADR-016 ("ConfigNodes record file existence at runtime, not
# documentation").
#
# Filter: .env.template exclusion (ADR-065 #4).
# Expected: zero EXTRACTED edges produced from this file. No ConfigNode
# created.

ICONS_API_BASE=
ICONS_AUTH_TOKEN=
